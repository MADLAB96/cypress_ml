{
  "name": "cypressml",
  "version": "1.0.0",
  "description": "scripts to build data for ml P.O.C.",
  "main": "index.ts",
  "scripts": {
    "start": "set NODE_ENV=dev && tsc && node index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "100": "set NODE_ENV=prod && tsc && node index.js 100",
    "500": "set NODE_ENV=prod && tsc && node index.js 500",
    "clean": "rm -r ./outputData/*",
    "build": "tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "typescript": "^3.6.4"
  },
  "devDependencies": {
    "@types/node": "^12.11.1"
  }
}
